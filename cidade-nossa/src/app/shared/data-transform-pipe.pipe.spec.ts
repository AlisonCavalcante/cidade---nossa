import { DataTransformPipePipe } from './data-transform-pipe.pipe';

describe('DataTransformPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DataTransformPipePipe();
    expect(pipe).toBeTruthy();
  });
});
